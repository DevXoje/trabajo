type DisplayOptions = "none" | "flex" | "block";
export type Display = {
  xs: DisplayOptions;
  md: DisplayOptions;
};
export type Orientation = {
  vertical: "top" | "bottom";
  horizontal: "left" | "right";
};
